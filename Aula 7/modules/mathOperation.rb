module MathOperation
    def sum(number1, number2)
        puts "Soma dos dois números: #{number1 + number2}"
    end

    def product(number1, number2)
        puts "Produto entre os dois produtos: #{number1 * number2}"
    end
end

